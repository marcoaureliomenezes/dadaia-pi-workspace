#!/usr/bin/env python3
from _workflow_common import main

main("backlog_definition.py", [
    "Backlog workflow policy: read demand, backlog, and bugs; run conflict review; ask bounded grill-me questions; write exactly one canonical backlog item only after ambiguity is resolved.",
])
