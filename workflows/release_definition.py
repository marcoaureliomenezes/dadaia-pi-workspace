#!/usr/bin/env python3
from _workflow_common import main

main("release_definition.py", [
    "Release definition policy: consume backlog and bugs, define scope, draft SPEC, PLAN, and grouped TASKS, then run an independent review loop with a hard iteration cap before writing approved artifacts.",
])
